# Integer-Sign-Segregation-System
A C-based data processing algorithm that classifies a user-defined stream of integers into distinct memory buffers based on their algebraic sign (Positive vs. Negative segregation).
